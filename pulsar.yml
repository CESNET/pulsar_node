- hosts: 127.0.0.1
  vars_files:
    - group_vars/secret.yml
  pre_tasks:
    - name: Install some packages
      package:
        name:
          - build-essential
          - git
          - python3-dev
          - libcurl4-openssl-dev
          - libssl-dev
          - virtualenv
          - libgnutls28-dev
          #- squashfs-tools # needed by singularity roles
        state: present
        update_cache: yes
      become: yes
  roles:
    #- role: galaxyproject.cvmfs
    #  become: yes
    #- gantsign.golang
    #- cyverse-ansible.singularity
    - galaxyproject.pulsar
